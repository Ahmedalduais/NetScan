name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: go install github.com/wailsapp/wails/v2/cmd/wails@latest
      - run: wails build -platform windows/amd64 -o NetScan.exe
      - uses: actions/upload-artifact@v4
        with:
          name: NetScan-Windows
          path: build/bin/NetScan.exe

  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common
          sudo add-apt-repository -y universe multiverse
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libgtk-3-dev
          sudo apt-get install -y libwebkit2gtk-4.0-dev || sudo apt-get install -y libwebkit2gtk-4.1-dev
      - run: go install github.com/wailsapp/wails/v2/cmd/wails@latest
      - run: wails build -platform linux/amd64 -o netscan
      - uses: actions/upload-artifact@v4
        with:
          name: NetScan-Linux
          path: build/bin/netscan

  build-macos:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: go install github.com/wailsapp/wails/v2/cmd/wails@latest
      - run: wails build -platform darwin/amd64 -o NetScan-Intel.app
      - uses: actions/upload-artifact@v4
        with:
          name: NetScan-macOS-Intel
          path: build/bin/NetScan-Intel.app

  build-macos-arm64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: go install github.com/wailsapp/wails/v2/cmd/wails@latest
      - run: wails build -platform darwin/arm64 -o NetScan-Silicon.app
      - uses: actions/upload-artifact@v4
        with:
          name: NetScan-macOS-Silicon
          path: build/bin/NetScan-Silicon.app

  release:
    needs: [build-windows, build-linux, build-macos, build-macos-arm64]
    runs-on: ubuntu-22.04
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
      - run: |
          mkdir release
          cp NetScan-Windows/NetScan.exe release/
          cp NetScan-Linux/netscan release/
          cp NetScan-macOS-Intel/NetScan-Intel.app release/
          cp NetScan-macOS-Silicon/NetScan-Silicon.app release/
      - uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
